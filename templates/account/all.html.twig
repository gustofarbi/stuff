{% extends 'base.html.twig' %}

{% block body %}
    <ul>
        {% for user in users %}
            <li>{{ user.id }} {{ user.email }} {{ user.password }}</li>
        {% endfor %}
    </ul>
{% endblock %}